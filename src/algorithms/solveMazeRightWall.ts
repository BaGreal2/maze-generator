import Cell from '../components/MazeField/cell.class';
import { mazeFunction } from '../types/mazeFunction.type';

export const solveMazeRightWall: mazeFunction<Cell[]> = async (
	field,
	setField,
	size,
	delay,
) => {
	return [];
};
